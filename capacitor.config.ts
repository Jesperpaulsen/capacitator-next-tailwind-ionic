import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'no.jesper.capacitatorTest',
  appName: 'capacitator-next-tailwind-ionic',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
